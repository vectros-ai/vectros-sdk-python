# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .org_request_status import OrgRequestStatus


class OrgRequest(UniversalBaseModel):
    """
    Request body for creating or updating an organization. On update (PUT), omitted fields are preserved — a null value does not clear a field — and the `payload` object is replaced in full when supplied, rather than deep-merged.
    """

    external_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="externalId"),
        pydantic.Field(
            alias="externalId",
            description="Your own unique identifier for this organization. Used for idempotent upsert: if an organization with this `externalId` already exists, it is returned instead of creating a duplicate.",
        ),
    ]
    name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Human-readable name for the organization.
    """

    status: typing.Optional[OrgRequestStatus] = pydantic.Field(default=None)
    """
    Lifecycle status of the organization. `ACTIVE` organizations can be used normally; `SUSPENDED` organizations are retained but blocked from new operations.
    """

    payload: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Free-form key-value attributes to store with the organization — use it to attach additional data from your own system. On update, the supplied object replaces the stored payload in full (it is not key-merged); omit it to leave the stored payload unchanged.
    """

    schema_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="schemaId"),
        pydantic.Field(
            alias="schemaId",
            description="Optional ID of a record schema (created via `POST /v1/schemas`) that governs payload validation and lookup indexing for this organization, and must belong to your account. When set, the payload is validated against the schema on save and the schema's declared lookup fields become queryable via `GET /v1/orgs?type=...&field=...&value=...`; when omitted, the payload is free-form and no lookup fields are indexed. Once set, this reference cannot currently be cleared.",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
