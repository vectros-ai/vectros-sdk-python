# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class LookupDef(UniversalBaseModel):
    """
    Declares a payload field that is indexed for direct lookup queries.
    """

    field_name: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="fieldName"),
        pydantic.Field(alias="fieldName", description="Name of the payload field to index."),
    ]
    unique: typing.Optional[bool] = pydantic.Field(default=None)
    """
    If true, enforces that this field's value is unique within your account.
    """

    range_enabled: typing_extensions.Annotated[
        typing.Optional[bool],
        FieldMetadata(alias="rangeEnabled"),
        pydantic.Field(
            alias="rangeEnabled",
            description="If true, this field also supports ordered range and prefix lookups (`from`/`to`/`prefix`) in addition to exact-match lookups. Range-enabled fields are billed at the range-index rate. Cannot be set on a sensitive field, because a blind index is not orderable. This setting is locked once the field is created.",
        ),
    ] = None
    sort_by: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="sortBy"),
        pydantic.Field(
            alias="sortBy",
            description="For an exact-match lookup, the index sort key: `createdAt` (the default), `lastUpdated`, or a declared field on this schema. This setting is locked once the field is created.",
        ),
    ] = None
    allow_overflow: typing_extensions.Annotated[
        typing.Optional[bool],
        FieldMetadata(alias="allowOverflow"),
        pydantic.Field(
            alias="allowOverflow",
            description="Each schema has a fixed budget of fast exact-match lookup indexes. An exact-match lookup beyond that budget is rejected unless you set this flag, which opts the field into a higher-cost secondary index (more storage and transactional writes). Range-enabled lookups do not count against the budget. This flag has no effect on a field that fits within the budget.",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
