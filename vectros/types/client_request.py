# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .client_request_status import ClientRequestStatus


class ClientRequest(UniversalBaseModel):
    """
    Request body for creating or updating a client. On update (PUT), omitted fields are preserved — a null does not clear a field — and `payload`, when supplied, replaces the stored payload in full rather than being deep-merged.
    """

    external_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="externalId"),
        pydantic.Field(
            alias="externalId",
            description="Your own unique identifier for this client. Used for idempotent create: if a client with this `externalId` already exists, it is returned instead of creating a duplicate.",
        ),
    ]
    name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Display name for this client.
    """

    status: typing.Optional[ClientRequestStatus] = pydantic.Field(default=None)
    """
    Lifecycle status of the client. `ACTIVE` clients can be used normally; `SUSPENDED` clients are retained but blocked from new operations. Defaults to `ACTIVE`.
    """

    org_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="orgId"),
        pydantic.Field(
            alias="orgId",
            description="The organization this client belongs to. Must be the Vectros-assigned UUID of an org created via `POST /v1/orgs`; use `GET /v1/orgs?externalId=` to resolve your own identifier to this UUID.",
        ),
    ] = None
    payload: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Free-form key-value attributes to store with this client — handy for keeping extra data from your system without defining a schema. On update, the supplied object replaces the stored payload in full (it is not key-merged); omit it to leave the payload unchanged.
    """

    schema_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="schemaId"),
        pydantic.Field(
            alias="schemaId",
            description="Optional ID of a schema (created via `POST /v1/schemas`) that validates this client's payload and drives its lookup indexing. When set, the schema must belong to your account, the payload is validated against the schema's fields on save, and the schema's declared lookup fields become queryable via `GET /v1/clients?type=...&field=...&value=...`. When omitted, the payload is free-form and no lookup rows are written. On update, omit this field to leave it unchanged; once set, it cannot be cleared.",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
