# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .scope_clause import ScopeClause


class RoleRequest(UniversalBaseModel):
    """
    Request body for creating or updating a role.
    """

    role_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="roleId"),
        pydantic.Field(
            alias="roleId",
            description="Your own identifier for the role. Must be 3 to 31 characters, starting with a lowercase letter and containing only lowercase letters, digits, and dashes (the same rules as a context ID). Required when creating a role; ignored when updating, since the role ID is taken from the URL path.",
        ),
    ]
    name: str = pydantic.Field()
    """
    A human-readable display name for the role.
    """

    description: typing.Optional[str] = pydantic.Field(default=None)
    """
    An optional free-text description of what the role grants.
    """

    scopes: typing.List[ScopeClause] = pydantic.Field()
    """
    The role's permissions, as one or more scope clauses. Each clause has `allowed_actions` (a list of permitted verbs) and `data_scope` (an attribute filter that restricts which records the actions apply to). An action is permitted if any clause allows that action and that clause's data scope matches the target record. To include records whose ownership field is null (account-level shared records), add `null` to the allowed values for that field.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
