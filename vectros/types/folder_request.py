# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class FolderRequest(UniversalBaseModel):
    """
    Request body for creating or updating a folder. On update (PUT or PATCH), omitted fields are preserved — a null does not clear a field. `parentFolderId` is honored only at creation and ignored on update, because a folder cannot currently be moved via the API.
    """

    name: str = pydantic.Field()
    """
    Human-readable display name of the folder. Required.
    """

    description: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional description of the folder's contents or purpose.
    """

    parent_folder_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="parentFolderId"),
        pydantic.Field(
            alias="parentFolderId",
            description="ID of the parent folder to create this folder under, applied at creation only. Omit to create the folder under your context's default root folder. Ignored on update: a folder cannot currently be moved via the API.",
        ),
    ] = None
    slug: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional stable slug for the folder, unique among its siblings under the same parent. Useful as an idempotency key so re-creating the same folder returns the existing one rather than a duplicate. Derived from the name when omitted. Must be lowercase letters, digits, and hyphens. Immutable once the folder is created.
    """

    user_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="userId"),
        pydantic.Field(
            alias="userId",
            description="Optional ID of the user that should own this folder — the Vectros-assigned UUID of a user in your account. With an API key, this sets the folder's owner directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.",
        ),
    ] = None
    org_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="orgId"),
        pydantic.Field(
            alias="orgId",
            description="Optional ID of the organization that should own this folder — the Vectros-assigned UUID of an organization in your account. With an API key, this sets the owning organization directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.",
        ),
    ] = None
    client_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="clientId"),
        pydantic.Field(
            alias="clientId",
            description="Optional ID of the client to associate with this folder — the Vectros-assigned UUID of a client in your account. With an API key, this sets the client directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.",
        ),
    ] = None
    expected_version: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="expectedVersion"),
        pydantic.Field(
            alias="expectedVersion",
            description="Optimistic-concurrency token for updates. Pass the `version` you last read to make the update conditional — it is rejected with a 409 version conflict (and the stored folder is left untouched) if the folder changed since. Omit for last-write-wins (the default). Ignored on create.",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
