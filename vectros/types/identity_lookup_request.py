# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .identity_lookup_request_order import IdentityLookupRequestOrder


class IdentityLookupRequest(UniversalBaseModel):
    """
    Request body for an identity lookup (user, organization, or client). Carries the lookup value in the body so a sensitive field's value never travels in the URL query string. Provide exactly one lookup mode: `value` (exact match), `from` plus `to` (range), or `prefix`.
    """

    type: str = pydantic.Field()
    """
    The identity schema type whose lookup fields you are querying (for example, `person_v1`).
    """

    field: str = pydantic.Field()
    """
    Name of the lookup field declared on the schema. For a field marked sensitive, this body variant is required (the GET variant rejects looking up by a sensitive value).
    """

    value: typing.Optional[str] = pydantic.Field(default=None)
    """
    Exact value to match. Mutually exclusive with `from`/`to` and `prefix`.
    """

    from_: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="from"),
        pydantic.Field(
            alias="from",
            description="Inclusive lower bound for a range lookup (requires `to`; supported only on range-enabled, non-sensitive fields). Mutually exclusive with `value` and `prefix`.",
        ),
    ] = None
    to: typing.Optional[str] = pydantic.Field(default=None)
    """
    Inclusive upper bound for a range lookup (requires `from`).
    """

    prefix: typing.Optional[str] = pydantic.Field(default=None)
    """
    Prefix to match (supported only on range-enabled string fields). Mutually exclusive with `value` and `from`/`to`.
    """

    start_from: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="startFrom"),
        pydantic.Field(
            alias="startFrom", description="Pagination cursor — pass the `nextCursor` returned by the previous page."
        ),
    ] = None
    limit: typing.Optional[int] = pydantic.Field(default=None)
    """
    Maximum number of results to return per page (1–100; defaults to 20).
    """

    order: typing.Optional[IdentityLookupRequestOrder] = pydantic.Field(default=None)
    """
    Sort direction for the returned results: `asc` (default) or `desc`.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
