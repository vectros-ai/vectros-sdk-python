# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .batch_write_result import BatchWriteResult


class BatchWriteResponse(UniversalBaseModel):
    """
    The result of a batch write: a per-item outcome list plus counts of how many items succeeded and failed. Returned with HTTP 200 even when some items failed, so always inspect the per-item results.
    """

    results: typing.Optional[typing.List[BatchWriteResult]] = pydantic.Field(default=None)
    """
    One result per item you submitted. Match each result to its request item by the `index` field, since results are not guaranteed to be in request order.
    """

    succeeded: typing.Optional[int] = pydantic.Field(default=None)
    """
    The number of items that were created or updated.
    """

    failed: typing.Optional[int] = pydantic.Field(default=None)
    """
    The number of items that failed (status `conflict` or `invalid`).
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
