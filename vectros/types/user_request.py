# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .user_request_status import UserRequestStatus
from .user_request_type import UserRequestType


class UserRequest(UniversalBaseModel):
    """
    Request body for creating or updating a user.
    """

    external_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="externalId"),
        pydantic.Field(
            alias="externalId",
            description="Your own unique identifier for this user. Drives idempotent upsert: if a user with this `externalId` already exists, it is returned instead of creating a duplicate.",
        ),
    ]
    email: typing.Optional[str] = pydantic.Field(default=None)
    """
    The user's email address. Used for display and notifications only; it is not used for authentication to the Vectros API.
    """

    status: typing.Optional[UserRequestStatus] = pydantic.Field(default=None)
    """
    The user's lifecycle status. `ACTIVE` users can be used normally; `SUSPENDED` users are retained but blocked from new operations. `PENDING` is a server-managed state for invitations awaiting acceptance — you cannot set it directly. (A pending user becomes `ACTIVE` by sending status=ACTIVE together with `inviteToken`, `externalSubject`, and `emailVerifiedAttestation=true`.) You may set only `ACTIVE` or `SUSPENDED` directly.
    """

    type: typing.Optional[UserRequestType] = pydantic.Field(default=None)
    """
    The kind of user. `HUMAN` (the default) is a real person; `SERVICE` is a bot, agent, scheduled job, or other named integration with no specific person behind it. Service users may omit `email`. The type is immutable after creation — to change it, create a new user.
    """

    payload: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Arbitrary key-value attributes to store on the user, useful for carrying additional data from your own system.
    """

    schema_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="schemaId"),
        pydantic.Field(
            alias="schemaId",
            description="Optional ID of a record schema (created via POST /v1/schemas) that drives payload validation and lookup indexing. When set, the schema must belong to your account, the payload is validated against the schema's fields on save, and the schema's lookup fields become queryable via GET /v1/users (using `type`, `field`, and a lookup mode). When omitted, the payload is free-form and no lookup rows are written.",
        ),
    ] = None
    invite_token: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="inviteToken"),
        pydantic.Field(
            alias="inviteToken",
            description="The raw invite token from the invitation email link. Required to activate a pending invitation (moving the user from PENDING to ACTIVE); ignored otherwise. The token's signature is verified server-side.",
        ),
    ] = None
    external_subject: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="externalSubject"),
        pydantic.Field(
            alias="externalSubject",
            description="The user's identifier in your own authentication system (for example, a Cognito sub or Auth0 user ID). Required to activate a pending invitation; ignored on subsequent updates. Treat it as immutable once the user is activated.",
        ),
    ] = None
    email_verified_attestation: typing_extensions.Annotated[
        typing.Optional[bool],
        FieldMetadata(alias="emailVerifiedAttestation"),
        pydantic.Field(
            alias="emailVerifiedAttestation",
            description="Your attestation that the invitee's email was verified by your authentication system before this call. Must be `true` to activate a pending invitation. Vectros cannot inspect your authentication system, so asserting this accurately is your responsibility.",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
