# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .chat_message_role import ChatMessageRole


class ChatMessage(UniversalBaseModel):
    """
    The conversation history, in order. A message with role `system` is extracted and used as the system prompt; `user` and `assistant` messages are sent to the model as conversation turns.
    """

    role: ChatMessageRole = pydantic.Field()
    """
    The role of this message. `system` is extracted and used as the system prompt; `user` and `assistant` are sent as conversation turns.
    """

    content: str = pydantic.Field()
    """
    The text content of the message.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
