# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .batch_lookup_result_status import BatchLookupResultStatus
from .record_response import RecordResponse


class BatchLookupResult(UniversalBaseModel):
    """
    The result for a single lookup in a batch-lookup request. Match it back to your input using the `ref` field.
    """

    ref: typing.Optional[str] = pydantic.Field(default=None)
    """
    The correlation id echoed from the lookup input that produced this result.
    """

    status: typing.Optional[BatchLookupResultStatus] = pydantic.Field(default=None)
    """
    The outcome for this lookup. `resolved` means exactly one match; `not_found` means no match; `ambiguous` means the field is not unique and matched more than one record.
    """

    data: typing.Optional[typing.List[RecordResponse]] = pydantic.Field(default=None)
    """
    The matched records. Empty unless `status` is `resolved` (one record) or `ambiguous` (more than one).
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
