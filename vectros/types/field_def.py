# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .field_def_field_type import FieldDefFieldType
from .field_def_target_surface import FieldDefTargetSurface


class FieldDef(UniversalBaseModel):
    """
    Defines a single field's type, validation, and indexing behavior.
    """

    field_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="fieldId"),
        pydantic.Field(alias="fieldId", description="Unique identifier for this field within the schema."),
    ]
    field_type: typing_extensions.Annotated[
        FieldDefFieldType,
        FieldMetadata(alias="fieldType"),
        pydantic.Field(
            alias="fieldType",
            description="Data type of the field. Use `reference` for a foreign-key field that points at another type — then set `targetTypeName`, `targetField`, `targetSurface`, and `cardinality` below.",
        ),
    ]
    required: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Whether this field is required when a record is created.
    """

    searchable: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Whether this field's value contributes to full-text search.
    """

    filterable: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Whether this field can be used as a filter in queries.
    """

    description: typing.Optional[str] = pydantic.Field(default=None)
    """
    Human-readable description of the field.
    """

    validation: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Validation rules for this field's value (such as minimum/maximum length or a regex pattern).
    """

    enum_values: typing_extensions.Annotated[
        typing.Optional[typing.List[typing.Dict[str, typing.Any]]],
        FieldMetadata(alias="enumValues"),
        pydantic.Field(alias="enumValues", description="The set of allowed values, for an enum-type field."),
    ] = None
    target_type_name: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="targetTypeName"),
        pydantic.Field(
            alias="targetTypeName",
            description="For a `reference` field: the target type this field points at. Required for reference fields.",
        ),
    ] = None
    target_field: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="targetField"),
        pydantic.Field(
            alias="targetField",
            description="For a `reference` field: the target's lookup field to resolve against. Defaults to `externalId`, and must name a unique lookup on the target type.",
        ),
    ] = None
    cardinality: typing.Optional[str] = pydantic.Field(default=None)
    """
    For a `reference` field: `one` (a single id, the default) or `many` (an array of ids).
    """

    target_surface: typing_extensions.Annotated[
        typing.Optional[FieldDefTargetSurface],
        FieldMetadata(alias="targetSurface"),
        pydantic.Field(
            alias="targetSurface",
            description="For a `reference` field: which surface the target lives on — record, document, user, org, or client. Required for reference fields. The same type name can exist on more than one surface, so this disambiguates which lookup resolves the reference.",
        ),
    ] = None
    sensitive: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Marks the field as sensitive (PHI/PII). Sensitive fields are redacted in logs, audit trails, and errors; blind-indexed for lookups; excluded from search; and masked in responses unless the token carries the `s` reveal scope for this record type.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
