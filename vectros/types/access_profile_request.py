# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .access_profile_request_status import AccessProfileRequestStatus
from .scope_clause import ScopeClause


class AccessProfileRequest(UniversalBaseModel):
    """
    Request body for creating or updating an access profile.
    """

    principal_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="principalId"),
        pydantic.Field(
            alias="principalId",
            description="Principal this profile applies to. Must start with `usr_` (an authenticated user — the suffix is the user id) or `key_` (a scoped API key acting as its own principal — the suffix is the key id). The suffix may contain only letters, digits, underscores, and hyphens. Required when creating (POST); ignored when updating (PUT), where it is taken from the path.",
        ),
    ]
    scopes: typing.Optional[typing.List[ScopeClause]] = pydantic.Field(default=None)
    """
    Inline scope clauses to grant the principal. Provide exactly one of `scopes` or `roleId` — setting both, or neither, returns a 400.
    """

    role_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="roleId"),
        pydantic.Field(
            alias="roleId",
            description="Reference to a role within the same context that supplies this principal's scopes. Provide exactly one of `scopes` or `roleId` — setting both, or neither, returns a 400. Changes to the role's scopes take effect for all referencing profiles.",
        ),
    ] = None
    identity_overrides: typing_extensions.Annotated[
        typing.Optional[typing.Dict[str, typing.Any]],
        FieldMetadata(alias="identityOverrides"),
        pydantic.Field(
            alias="identityOverrides",
            description="Optional per-context identity overrides. Only `orgId` and `clientId` may be overridden; any other key is rejected. An empty or omitted map applies no overrides — the base identity from the user or key record is used as-is.",
        ),
    ] = None
    status: typing.Optional[AccessProfileRequestStatus] = pydantic.Field(default=None)
    """
    Profile lifecycle status. `active` permits token minting; `suspended` denies it (minting returns a uniform 403). Defaults to `active` when omitted.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
