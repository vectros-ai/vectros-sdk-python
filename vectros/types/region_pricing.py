# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .region_rates import RegionRates


class RegionPricing(UniversalBaseModel):
    """
    Per-region credit rates (per 1K tokens) for a model.
    """

    base: RegionRates
    us: RegionRates
    region_premium_factor: typing_extensions.Annotated[
        float,
        FieldMetadata(alias="regionPremiumFactor"),
        pydantic.Field(
            alias="regionPremiumFactor",
            description="Premium multiplier applied to the US rate (1.0 or greater). A value of 1.0 means the US and base rates are equal — no premium.",
        ),
    ]
    global_available: typing_extensions.Annotated[
        bool,
        FieldMetadata(alias="globalAvailable"),
        pydantic.Field(
            alias="globalAvailable",
            description="Whether this model can be served from a non-US region when you opt into global data residency.",
        ),
    ]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
