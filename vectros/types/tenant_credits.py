# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class TenantCredits(UniversalBaseModel):
    """
    Environment-scoped credit total (no limit/breakdown — those live at the account level)
    """

    used: typing.Optional[int] = pydantic.Field(default=None)
    """
    Credits consumed by this environment this period (rounded down)
    """

    used_milli: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="usedMilli"),
        pydantic.Field(
            alias="usedMilli",
            description="used in milli-credit precision (exact, 1 credit = 1000 milli-credits). Use this for exact accounting — `tenants.live.credits.used + tenants.test.credits.used` can differ from the account-level `credits.used` by up to 1 credit due to independent truncation, but `live.usedMilli + test.usedMilli == credits.usedMilli` always.",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
