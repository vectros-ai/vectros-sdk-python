# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .record_request_index_mode import RecordRequestIndexMode


class RecordRequest(UniversalBaseModel):
    """
    Request body for creating or updating a record. Update semantics (PUT): omitted fields are preserved — sending null does not clear a field; `payload` is replaced in full when supplied (it is not deep-merged); and `typeName` and `schemaId` are immutable after creation and ignored on update.
    """

    type_name: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="typeName"),
        pydantic.Field(
            alias="typeName",
            description="The record type, matching a schema's record type. Provide `typeName` or `schemaId` (at least one is required); when only `schemaId` is given, the type is resolved from the schema, and when both are given they must agree. Immutable after creation and ignored on update.",
        ),
    ] = None
    schema_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="schemaId"),
        pydantic.Field(
            alias="schemaId",
            description="Identifier of the schema to validate against. Optional: omit it and the server resolves the schema from `typeName` (record types are unique within your account and context); supply it (and omit `typeName`) to pin a schema and resolve the type from it. Provide `typeName` or `schemaId` (at least one); when both are given the server checks that they agree. Immutable after creation and ignored on update.",
        ),
    ] = None
    payload: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    The record's data payload — a JSON object whose structure is validated against the referenced schema. Fields the schema marks as searchable contribute to full-text and semantic search. On update, the supplied object replaces the stored payload in full (it is not key-merged); omit it to leave the payload unchanged.
    """

    status: typing.Optional[str] = pydantic.Field(default=None)
    """
    Record lifecycle status. Defaults to ACTIVE. Use it to model soft-delete or workflow states without physically deleting records.
    """

    folder_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="folderId"),
        pydantic.Field(
            alias="folderId",
            description="Identifier of the folder to group this record with related documents. On create, omit to leave the record ungrouped. On update, omit to leave it unchanged; once set, this field cannot be cleared.",
        ),
    ] = None
    user_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="userId"),
        pydantic.Field(
            alias="userId",
            description="Identifier of the owning user — the Vectros-assigned UUID of a user in your account. Optional, and may be set automatically from the calling token's identity. Use `GET /v1/users?externalId=` to resolve the UUID from your own identifier.",
        ),
    ] = None
    org_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="orgId"),
        pydantic.Field(
            alias="orgId",
            description="Identifier of the owning organization — the Vectros-assigned UUID of an organization in your account. Optional, and may be set automatically from the calling token's identity. Use `GET /v1/orgs?externalId=` to resolve the UUID from your own identifier.",
        ),
    ] = None
    client_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="clientId"),
        pydantic.Field(
            alias="clientId",
            description="Identifier of the associated client — the Vectros-assigned UUID of a client in your account. Optional, and may be set automatically from the calling token's identity. Use `GET /v1/clients?externalId=` to resolve the UUID from your own identifier.",
        ),
    ] = None
    external_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="externalId"),
        pydantic.Field(
            alias="externalId",
            description="Your own stable identifier for this record. Optional, and immutable after create. Unique within your account, context, and record type: posting again with the same `externalId` returns the existing record (idempotent create), and it is the key other records reference. Maximum 256 characters.",
        ),
    ] = None
    index_mode: typing_extensions.Annotated[
        typing.Optional[RecordRequestIndexMode],
        FieldMetadata(alias="indexMode"),
        pydantic.Field(
            alias="indexMode",
            description="Per-record override of the search-index mode. HYBRID, SEMANTIC, and TEXT index this record for search; NONE is store-only (persisted, retrievable by id, and findable by structured-field lookup, but not searchable). Optional — omit it to inherit the schema's type-level default index mode (which itself defaults to NONE when the schema declares none). When both are set, this per-record value wins. Immutable after create.",
        ),
    ] = None
    expected_version: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="expectedVersion"),
        pydantic.Field(
            alias="expectedVersion",
            description="Optimistic-concurrency token. Pass the `version` you last read (from a fetch or a prior write response) to make this update conditional — it is rejected with 409 VERSION_CONFLICT if the record was modified since, leaving the stored record untouched. Omit it for last-write-wins (the default). Ignored on create.",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
